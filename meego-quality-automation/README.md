# 飞书项目(Meego)质量指标自动化配置系统

> 🚀 **配置即代码** - 告别手动配置的痛苦，一键同步5个质量指标到飞书项目

## 🎯 解决的问题

- ❌ **手动配置繁琐**：在UI中逐个配置字段和流程节点费时费力
- ❌ **配置不可复用**：无法在多个项目空间复用相同配置
- ❌ **难以版本控制**：UI配置无法追踪变更历史
- ❌ **容易出错**：手动操作容易遗漏或配置错误

## ✨ 核心特性

- 📝 **YAML配置管理**：所有配置通过YAML文件管理，清晰易懂
- 🔄 **幂等性设计**：重复执行不会产生副作用，安全可靠
- 🔍 **Chrome DevTools集成**：自动调试API，无需手动Postman
- 📊 **5个质量指标**：预配置产品质量监控的核心指标
- ⚡ **一键同步**：单个命令完成所有配置

## 📋 预配置的5个质量指标

1. **需求Lead Time** - 从需求创建到上线的平均时长
2. **评审一次通过率** - 第一次评审就通过的需求占比
3. **并行事项吞吐量** - 团队同时处理的需求数量
4. **PRD返工率** - 产品需求文档被打回修改的比例
5. **试点到GA迭代次数** - 从试点到全面推广所需的迭代数

## 🚀 快速开始

### 1. 获取飞书项目凭据

#### 步骤1：创建插件应用
```
1. 访问飞书项目管理后台
2. 进入"开发者" -> "创建插件"
3. 记录 Plugin ID 和 Plugin Secret
```

#### 步骤2：获取用户Key
```
飞书客户端 -> 双击左下角头像 -> 复制 User Key
```

#### 步骤3：获取项目Key
```
进入项目空间 -> 双击空间名称 -> 复制 Project Key
```

### 2. 配置认证信息

```bash
# 复制认证模板
cp credentials.yaml.example credentials.yaml

# 编辑认证文件
vim credentials.yaml
```

填入您的凭据：
```yaml
plugin_id: "your_plugin_id"
plugin_secret: "your_plugin_secret"
user_key: "your_user_key"
```

### 3. 修改质量指标配置

编辑 `quality-metrics.yaml`：
```yaml
project:
  key: "your_project_key"  # 替换为您的项目key
  name: "产品质量监控"
```

### 4. 执行同步

```bash
# 安装依赖
pip install -r requirements.txt

# 执行同步
python sync_config.py
```

## 🔧 高级用法

### 使用Chrome DevTools调试API

```bash
# 启动调试模式
python sync_config.py --debug

# 或直接运行调试工具
python mcp_debugger.py
```

### 生成API客户端代码

```bash
# 自动捕获API并生成客户端
python mcp_debugger.py
# 输出: generated_client.py
```

### 环境变量配置

除了YAML文件，也支持环境变量：
```bash
export FEISHU_PLUGIN_ID=your_plugin_id
export FEISHU_PLUGIN_SECRET=your_plugin_secret
export FEISHU_USER_KEY=your_user_key

python sync_config.py
```

## 📁 项目结构

```
meego-quality-automation/
├── quality-metrics.yaml        # 质量指标配置（核心）
├── sync_config.py              # 主同步脚本
├── mcp_debugger.py            # Chrome DevTools调试工具
├── credentials.yaml.example    # 认证配置模板
├── requirements.txt           # Python依赖
├── quick_start.sh            # 快速启动脚本(Linux/Mac)
├── quick_start.bat           # 快速启动脚本(Windows)
└── README.md                 # 本文档
```

## 🎨 配置文件说明

### quality-metrics.yaml

定义了5个质量指标的详细配置：

```yaml
quality_metrics:
  - key: "requirement_lead_time"
    name: "需求Lead Time"
    fields:
      - key: "requirement_created_at"
        name: "需求创建时间"
        type: "datetime"
    formula: "DATEDIFF(deployed_at, requirement_created_at, 'day')"
    target: "≤30天"
```

每个指标包含：
- `key`: 唯一标识
- `name`: 显示名称
- `fields`: 相关字段列表
- `formula`: 计算公式
- `target`: 目标值
- `threshold`: 阈值配置（优秀/良好/警告/严重）

### 流程节点配置

```yaml
workflow_nodes:
  - key: "requirement"
    name: "需求"
    type: "start"
    required_fields: ["requirement_created_at"]
```

定义了6个流程节点：需求 → 方案 → 评审 → 开发 → 试点 → GA上线

## 🔄 幂等性保证

脚本设计为幂等操作：
- ✅ 字段已存在时跳过创建
- ✅ 配置相同时不会重复更新
- ✅ 每个请求包含幂等UUID
- ✅ 可以安全地重复执行

## 📊 执行结果

成功执行后，您将在飞书项目中看到：

1. **自定义字段**：26个质量相关字段
2. **流程节点**：6个标准流程节点
3. **自动化规则**：3个自动计算规则（需UI确认）

## 🐛 故障排查

### 常见问题

1. **认证失败**
   ```
   检查 Plugin ID/Secret 是否正确
   确认插件已安装到项目空间
   ```

2. **字段创建失败**
   ```
   可能字段key已存在，检查现有字段
   确认有项目管理员权限
   ```

3. **API限流**
   ```
   脚本已内置延迟，如仍触发限流可调整 time.sleep() 时间
   ```

### 调试模式

启用详细日志：
```bash
export LOG_LEVEL=DEBUG
python sync_config.py
```

## 🤝 贡献

欢迎提交Issue和PR！

## 📄 许可

MIT License

---

**💡 提示**：首次运行建议先在测试项目空间验证，确认无误后再应用到生产环境。